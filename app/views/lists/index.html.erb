<h1>My Lists</h1>
  <hr class="featurette-divider">
  <% if @lists.present? %>
    <% @lists.each do |list| %>
      <ul class ='list'>
        <li>
          <%= link_to list.description, list %>
        </li>
      </ul>
    <% end %>
  <% else %>
    <p class="lead"> You don't have any list created </p>
  <% end %>

<div class="well">
  <%= link_to 'Add a new list', new_list_path, :class => 'btn btn-primary' %>
</div>

